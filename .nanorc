# Add syntax highlighting
include /usr/share/nano/*

# Don't wrap text at all.
set nowrap

# Use a tab size of n columns. The value of n must be greater than 0. The default value is 8.
set tabsize 4

# Use auto-indentation.
set autoindent

# Constantly display the cursor position in the status bar.
set constantshow

# Make the Home key smarter. When Home is pressed anywhere but at the very beginning of non-
# whitespace characters on a line, the cursor will jump to that beginning (either forwards
# or backwards). If the cursor is already at that position, it will jump to the true beginning of the line.
set smarthome

# Convert typed tabs to spaces.
set tabstospaces
